version: '3.8'

services:
  bot:
    volumes:
      - ./app:/app/app
    environment:
      DEBUG: "true"
    command: python -m app.main

  api:
    volumes:
      - ./app:/app/app
    environment:
      DEBUG: "true"
    command: uvicorn app.api.main:app --reload --host 0.0.0.0 --port 8000
